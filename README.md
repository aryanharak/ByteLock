# ByteLock
ByteLock is a simple, browser‑based file locker that uses AES‑256‑GCM to encrypt and decrypt any file locally. Drop a file, set a password, and download the locked or unlocked version—no uploads, no servers, just client‑side crypto.
